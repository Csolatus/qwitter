    <!-- Sidebar (Navigation) -->
    <aside class="w-64 bg-white border-r border-gray-200 flex flex-col justify-between fixed h-full z-10">
        <div class="p-6">
            <!-- Logo -->
            <div class="mb-8 flex items-center gap-3">
                <div class="w-8 h-8 bg-slate-800 rounded-lg flex items-center justify-center text-white font-bold">Q</div>
                <span class="text-xl font-bold tracking-tight text-slate-800">Qwitter</span>
            </div>

            <!-- Menu Principal -->
            <nav class="space-y-1">
                <a href="{{ path('app_accueil') }}" class="flex items-center gap-3 px-4 py-3 {{ app.request.attributes.get('_route') == 'app_accueil' ? 'bg-slate-800 text-white shadow-lg shadow-slate-200' : 'text-gray-500 hover:bg-gray-50 hover:text-slate-800' }} rounded-xl transition-all group">
                    <div class="w-5 h-5 {{ app.request.attributes.get('_route') == 'app_accueil' ? 'bg-white/20' : 'bg-gray-200' }} rounded-md group-hover:bg-slate-200 transition-colors"></div>
                    <span class="font-medium">Accueil</span>
                </a>
                <a href="#" class="flex items-center gap-3 px-4 py-3 text-gray-500 hover:bg-gray-50 hover:text-slate-800 rounded-xl transition-all group">
                    <div class="w-5 h-5 bg-gray-200 rounded-md group-hover:bg-slate-200 transition-colors"></div>
                    <span class="font-medium">Découvrir</span>
                </a>
                <a href="#" class="flex items-center gap-3 px-4 py-3 text-gray-500 hover:bg-gray-50 hover:text-slate-800 rounded-xl transition-all group">
                    <div class="w-5 h-5 bg-gray-200 rounded-md group-hover:bg-slate-200 transition-colors"></div>
                    <span class="font-medium">Messages</span>
                </a>
                <a href="#" class="flex items-center gap-3 px-4 py-3 text-gray-500 hover:bg-gray-50 hover:text-slate-800 rounded-xl transition-all group">
                    <div class="w-5 h-5 bg-gray-200 rounded-md group-hover:bg-slate-200 transition-colors"></div>
                    <span class="font-medium">Notifications</span>
                </a>
            </nav>
        </div>

        <div class="p-6">
            <!-- Séparateur -->
            <div class="h-px bg-gray-100 mb-6"></div>

            <!-- Menu Bas -->
            <nav class="space-y-1">
                <a href="{{ app.user ? path('app_profile') : path('home') }}" class="flex items-center gap-3 px-4 py-3 {{ app.request.attributes.get('_route') == 'app_profile' ? 'bg-slate-800 text-white shadow-lg shadow-slate-200' : 'text-gray-500 hover:bg-gray-50 hover:text-slate-800' }} rounded-xl transition-all group">
                    <div class="w-5 h-5 {{ app.request.attributes.get('_route') == 'app_profile' ? 'bg-white/20' : 'bg-gray-200' }} rounded-md group-hover:bg-slate-200 transition-colors"></div>
                    <span class="font-medium">Profil</span>
                </a>
                <a href="#" class="flex items-center gap-3 px-4 py-3 text-gray-500 hover:bg-gray-50 hover:text-slate-800 rounded-xl transition-all group">
                    <div class="w-5 h-5 bg-gray-200 rounded-md group-hover:bg-slate-200 transition-colors"></div>
                    <span class="font-medium">Nous Contacter</span>
                </a>
                <!-- Lien Actif -->
                <a href="{{ path('app_parametres') }}" class="flex items-center gap-3 px-4 py-3 {{ app.request.attributes.get('_route') == 'app_parametres' ? 'bg-slate-800 text-white shadow-lg shadow-slate-200' : 'text-gray-500 hover:bg-gray-50 hover:text-slate-800' }} rounded-xl transition-all">
                    <div class="w-5 h-5 {{ app.request.attributes.get('_route') == 'app_parametres' ? 'bg-white/20' : 'bg-gray-200' }} rounded-md"></div>
                    <span class="font-medium">Paramètres</span>
                </a>
            </nav>

            <!-- Bouton Poster / Action -->
            <button class="mt-8 w-full bg-slate-800 text-white py-3 rounded-xl font-medium shadow-lg shadow-slate-200 hover:bg-slate-900 transition-colors flex items-center justify-center gap-2">
                <div class="w-4 h-4 bg-white/20 rounded flex items-center justify-center text-xs">+</div>
                <span>Poster</span>
            </button>
        </div>
    </aside>
