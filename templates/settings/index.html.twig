{% extends 'base.html.twig' %}

{% block title %}Paramètres - Qwitter{% endblock %}

{% block body %}
    <div class="max-w-5xl mx-auto space-y-10">
        
        <!-- Header de la page -->
        <header class="space-y-4">
            <h1 class="text-3xl font-bold text-slate-800">Paramètres Généraux</h1>
            
            <!-- Navigation Tabulaire -->
            <div class="flex gap-4 border-b border-gray-200 pb-1 mt-6">
                <a href="{{ path('app_parametres', {tab: 'account'}) }}" class="pb-3 border-b-2 {{ onglet_actif == 'account' ? 'border-slate-800 text-slate-800 font-semibold' : 'border-transparent text-gray-400 hover:text-gray-600 font-medium' }} px-2 transition-colors">Mon Compte</a>
                <a href="{{ path('app_parametres', {tab: 'privacy'}) }}" class="pb-3 border-b-2 {{ onglet_actif == 'privacy' ? 'border-slate-800 text-slate-800 font-semibold' : 'border-transparent text-gray-400 hover:text-gray-600 font-medium' }} px-2 transition-colors">Confidentialité</a>
                <a href="{{ path('app_parametres', {tab: 'security'}) }}" class="pb-3 border-b-2 {{ onglet_actif == 'security' ? 'border-slate-800 text-slate-800 font-semibold' : 'border-transparent text-gray-400 hover:text-gray-600 font-medium' }} px-2 transition-colors">Sécurité</a>
                <a href="{{ path('app_parametres', {tab: 'billing'}) }}" class="pb-3 border-b-2 {{ onglet_actif == 'billing' ? 'border-slate-800 text-slate-800 font-semibold' : 'border-transparent text-gray-400 hover:text-gray-600 font-medium' }} px-2 transition-colors">Facturation</a>
            </div>
        </header>

        <!-- Contenu dynamique selon l'onglet -->
        {% if onglet_actif == 'account' %}
            <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-8">
                {{ form_start(formulaire, {'attr': {'class': 'space-y-8'}}) }}

                <!-- Section Avatar -->
                <div class="flex items-center gap-6">
                    <div class="relative group">
                        <div class="w-24 h-24 bg-slate-100 rounded-full flex items-center justify-center overflow-hidden border-4 border-white shadow-lg">
                            {% if app.user.avatar %}
                                <img src="{{ asset('uploads/avatars/' ~ app.user.avatar) }}" alt="Avatar de {{ app.user.pseudo }}" class="w-full h-full object-cover">
                            {% else %}
                                <span class="text-3xl">👤</span>
                            {% endif %}
                        </div>
                        <label for="{{ formulaire.avatar.vars.id }}" class="absolute bottom-0 right-0 bg-slate-800 text-white p-2 rounded-full cursor-pointer hover:bg-slate-700 transition-colors shadow-md">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z" />
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 13a3 3 0 11-6 0 3 3 0 016 0z" />
                            </svg>
                        </label>
                    </div>
                    <div>
                        <h3 class="text-lg font-bold text-slate-800">Votre Photo</h3>
                        <p class="text-sm text-gray-500 mb-2">Cette photo sera affichée sur votre profil.</p>
                        <div class="hidden">
                            {{ form_widget(formulaire.avatar) }}
                        </div>
                        {{ form_errors(formulaire.avatar) }}
                    </div>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <!-- Pseudo -->
                    <div class="space-y-2">
                        {{ form_label(formulaire.pseudo, null, {'label_attr': {'class': 'block text-sm font-semibold text-slate-700'}}) }}
                        {{ form_widget(formulaire.pseudo, {'attr': {'class': 'w-full px-4 py-2 rounded-xl border border-gray-200 focus:border-slate-500 focus:ring-2 focus:ring-slate-200 outline-none transition-all', 'placeholder': 'Votre pseudo'}}) }}
                        {{ form_errors(formulaire.pseudo, {'attr': {'class': 'text-red-500 text-sm'}}) }}
                    </div>

                    <!-- Email -->
                    <div class="space-y-2">
                        {{ form_label(formulaire.email, null, {'label_attr': {'class': 'block text-sm font-semibold text-slate-700'}}) }}
                        {{ form_widget(formulaire.email, {'attr': {'class': 'w-full px-4 py-2 rounded-xl border border-gray-200 focus:border-slate-500 focus:ring-2 focus:ring-slate-200 outline-none transition-all', 'placeholder': 'exemple@email.com'}}) }}
                        {{ form_errors(formulaire.email, {'attr': {'class': 'text-red-500 text-sm'}}) }}
                    </div>
                </div>

                <!-- Bio -->
                <div class="space-y-2">
                    {{ form_label(formulaire.bio, null, {'label_attr': {'class': 'block text-sm font-semibold text-slate-700'}}) }}
                    {{ form_widget(formulaire.bio, {'attr': {'class': 'w-full px-4 py-2 rounded-xl border border-gray-200 focus:border-slate-500 focus:ring-2 focus:ring-slate-200 outline-none transition-all resize-y', 'rows': '4', 'placeholder': 'Dites quelque chose sur vous...'}}) }}
                    {{ form_errors(formulaire.bio, {'attr': {'class': 'text-red-500 text-sm'}}) }}
                </div>

                <!-- Actions -->
                <div class="flex items-center justify-end gap-4 pt-4 border-t border-gray-100">
                    <button type="button" class="px-6 py-2.5 rounded-xl text-slate-600 hover:bg-slate-50 font-medium transition-colors">Annuler</button>
                    <button type="submit" class="px-6 py-2.5 rounded-xl bg-slate-800 text-white font-medium hover:bg-slate-900 shadow-lg shadow-slate-200 transition-all flex items-center gap-2">
                        <span>Enregistrer les modifications</span>
                    </button>
                </div>

                {{ form_end(formulaire) }}
            </div>

        {% elseif onglet_actif == 'privacy' %}
            <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-8">
                {{ form_start(formulaire, {'attr': {'class': 'space-y-8'}}) }}
                
                <div class="space-y-6">
                    <h3 class="text-lg font-bold text-slate-800 border-b border-gray-100 pb-4 mb-6">Visibilité et Contact</h3>

                    <!-- Compte Privé -->
                    <div class="flex items-center justify-between p-4 bg-gray-50 rounded-xl border border-gray-100">
                        <div>
                            {{ form_label(formulaire.isPrivate, null, {'label_attr': {'class': 'font-semibold text-slate-800'}}) }}
                            <p class="text-sm text-gray-500">{{ formulaire.isPrivate.vars.help }}</p>
                        </div>
                        <div class="flex items-center">
                             {{ form_widget(formulaire.isPrivate, {'attr': {'class': 'w-5 h-5 text-slate-600 rounded focus:ring-slate-500 border-gray-300'}}) }}
                        </div>
                    </div>

                    <!-- Messages Privés -->
                    <div class="space-y-2">
                        {{ form_label(formulaire.messagePrivacy) }}
                        {{ form_widget(formulaire.messagePrivacy) }}
                        {{ form_errors(formulaire.messagePrivacy) }}
                    </div>

                    <h3 class="text-lg font-bold text-slate-800 border-b border-gray-100 pb-4 pt-4 mb-6">Autres Paramètres</h3>

                    <!-- Statut en ligne -->
                    <div class="flex items-center justify-between p-4 bg-gray-50 rounded-xl border border-gray-100">
                        <div>
                            {{ form_label(formulaire.isOnlineVisible, null, {'label_attr': {'class': 'font-semibold text-slate-800'}}) }}
                            <p class="text-sm text-gray-500">{{ formulaire.isOnlineVisible.vars.help }}</p>
                        </div>
                        <div class="flex items-center">
                             {{ form_widget(formulaire.isOnlineVisible, {'attr': {'class': 'w-5 h-5 text-slate-600 rounded focus:ring-slate-500 border-gray-300'}}) }}
                        </div>
                    </div>

                    <!-- Référencement -->
                    <div class="flex items-center justify-between p-4 bg-gray-50 rounded-xl border border-gray-100">
                        <div>
                            {{ form_label(formulaire.isIndexed, null, {'label_attr': {'class': 'font-semibold text-slate-800'}}) }}
                            <p class="text-sm text-gray-500">{{ formulaire.isIndexed.vars.help }}</p>
                        </div>
                        <div class="flex items-center">
                             {{ form_widget(formulaire.isIndexed, {'attr': {'class': 'w-5 h-5 text-slate-600 rounded focus:ring-slate-500 border-gray-300'}}) }}
                        </div>
                    </div>
                </div>

                <!-- Actions -->
                <div class="flex items-center justify-end gap-4 pt-4 border-t border-gray-100">
                    <button type="button" class="px-6 py-2.5 rounded-xl text-slate-600 hover:bg-slate-50 font-medium transition-colors">Annuler</button>
                    <button type="submit" class="px-6 py-2.5 rounded-xl bg-slate-800 text-white font-medium hover:bg-slate-900 shadow-lg shadow-slate-200 transition-all flex items-center gap-2">
                        <span>Enregistrer les préférences</span>
                    </button>
                </div>

                {{ form_end(formulaire) }}
            </div>

        {% elseif onglet_actif == 'security' %}
            <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-8">
                {{ form_start(formulaire, {'attr': {'class': 'space-y-8'}}) }}

                <!-- Changement de mot de passe -->
                <div class="space-y-6">
                    <h3 class="text-lg font-bold text-slate-800 border-b border-gray-100 pb-4 mb-6">Mot de passe</h3>
                    
                    <div class="space-y-4">
                        <div class="space-y-2">
                            {{ form_label(formulaire.currentPassword) }}
                            {{ form_widget(formulaire.currentPassword) }}
                            {{ form_errors(formulaire.currentPassword) }}
                        </div>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div class="space-y-2">
                                {{ form_label(formulaire.newPassword.first) }}
                                {{ form_widget(formulaire.newPassword.first) }}
                                {{ form_errors(formulaire.newPassword.first) }}
                            </div>
                            <div class="space-y-2">
                                {{ form_label(formulaire.newPassword.second) }}
                                {{ form_widget(formulaire.newPassword.second) }}
                                {{ form_errors(formulaire.newPassword.second) }}
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 2FA -->
                <div class="space-y-6 pt-4">
                    <h3 class="text-lg font-bold text-slate-800 border-b border-gray-100 pb-4 mb-6">Double Authentification</h3>
                    
                    <div class="flex items-center justify-between p-4 bg-gray-50 rounded-xl border border-gray-100">
                        <div>
                            {{ form_label(formulaire.is2FAEnabled, null, {'label_attr': {'class': 'font-semibold text-slate-800'}}) }}
                            <p class="text-sm text-gray-500">{{ formulaire.is2FAEnabled.vars.help }}</p>
                        </div>
                        <div class="flex items-center">
                             {{ form_widget(formulaire.is2FAEnabled, {'attr': {'class': 'w-5 h-5 text-slate-600 rounded focus:ring-slate-500 border-gray-300'}}) }}
                        </div>
                    </div>
                </div>

                <!-- Historique de connexion (Statique pour l'instant) -->
                <div class="space-y-6 pt-4">
                     <h3 class="text-lg font-bold text-slate-800 border-b border-gray-100 pb-4 mb-6">Historique de connexion</h3>
                     <div class="space-y-4">
                        <div class="flex items-center justify-between p-4 bg-white border border-gray-100 rounded-xl">
                            <div class="flex items-center gap-4">
                                <div class="w-10 h-10 bg-green-100 text-green-600 rounded-lg flex items-center justify-center">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                                    </svg>
                                </div>
                                <div>
                                    <p class="font-semibold text-slate-800">MacBook Pro - Paris, FR</p>
                                    <p class="text-sm text-green-600">Actif maintenant</p>
                                </div>
                            </div>
                            <span class="text-sm text-gray-400">IP: 192.168.1.1</span>
                        </div>
                     </div>
                </div>

                <!-- Actions -->
                <div class="flex items-center justify-end gap-4 pt-4 border-t border-gray-100">
                    <button type="button" class="px-6 py-2.5 rounded-xl text-slate-600 hover:bg-slate-50 font-medium transition-colors">Annuler</button>
                    <button type="submit" class="px-6 py-2.5 rounded-xl bg-slate-800 text-white font-medium hover:bg-slate-900 shadow-lg shadow-slate-200 transition-all flex items-center gap-2">
                        <span>Mettre à jour la sécurité</span>
                    </button>
                </div>

                {{ form_end(formulaire) }}
            </div>

        {% elseif onglet_actif == 'billing' %}
            <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-8">
                <!-- Mon Abonnement -->
                <div class="space-y-6">
                    <h3 class="text-lg font-bold text-slate-800 border-b border-gray-100 pb-4 mb-6">Mon Abonnement</h3>
                    
                    <div class="bg-slate-50 rounded-xl p-6 border border-slate-100 flex items-center justify-between">
                        <div>
                            <span class="inline-block px-3 py-1 bg-slate-200 text-slate-700 text-xs font-bold rounded-full mb-2">ACTUEL</span>
                            <h4 class="text-xl font-bold text-slate-800">Plan Gratuit</h4>
                            <p class="text-gray-500 mt-1">Accès basique aux fonctionnalités de Qwitter.</p>
                        </div>
                        <button class="px-6 py-2.5 rounded-xl bg-slate-800 text-white font-medium hover:bg-slate-900 shadow-lg shadow-slate-200 transition-all">
                            Passer Premium
                        </button>
                    </div>
                </div>

                <!-- Méthode de paiement -->
                <div class="space-y-6 pt-8">
                    <h3 class="text-lg font-bold text-slate-800 border-b border-gray-100 pb-4 mb-6">Moyen de paiement</h3>
                    
                    <div class="flex items-center justify-between p-4 bg-white border border-gray-200 rounded-xl">
                        <div class="flex items-center gap-4">
                            <div class="w-12 h-8 bg-slate-800 rounded flex items-center justify-center text-white text-xs font-bold tracking-widest">VISA</div>
                            <div>
                                <p class="font-semibold text-slate-800">Visa terminant par 4242</p>
                                <p class="text-sm text-gray-400">Expire le 12/28</p>
                            </div>
                        </div>
                        <button class="text-sm font-medium text-slate-600 hover:text-slate-800">Modifier</button>
                    </div>
                    
                    <button class="text-sm font-medium text-slate-600 hover:text-slate-800 flex items-center gap-2 mt-4">
                        <span>+ Ajouter une nouvelle carte</span>
                    </button>
                </div>

                <!-- Factures -->
                 <div class="space-y-6 pt-8">
                    <h3 class="text-lg font-bold text-slate-800 border-b border-gray-100 pb-4 mb-6">Historique des factures</h3>
                    
                    <div class="border border-gray-100 rounded-xl overflow-hidden">
                        <table class="w-full text-left text-sm text-gray-600">
                            <thead class="bg-gray-50 text-gray-500 font-medium border-b border-gray-100">
                                <tr>
                                    <th class="px-6 py-3">Date</th>
                                    <th class="px-6 py-3">Montant</th>
                                    <th class="px-6 py-3">Statut</th>
                                    <th class="px-6 py-3 text-right">Action</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-100">
                                <tr class="hover:bg-gray-50 transition-colors">
                                    <td class="px-6 py-4">14 Jan 2026</td>
                                    <td class="px-6 py-4 font-medium text-slate-800">0,00 €</td>
                                    <td class="px-6 py-4"><span class="px-2 py-1 bg-green-100 text-green-700 rounded text-xs font-bold">Payé</span></td>
                                    <td class="px-6 py-4 text-right"><a href="#" class="text-slate-600 hover:text-slate-800 hover:underline">Télécharger</a></td>
                                </tr>
                                <tr class="hover:bg-gray-50 transition-colors">
                                    <td class="px-6 py-4">14 Dec 2025</td>
                                    <td class="px-6 py-4 font-medium text-slate-800">0,00 €</td>
                                    <td class="px-6 py-4"><span class="px-2 py-1 bg-green-100 text-green-700 rounded text-xs font-bold">Payé</span></td>
                                    <td class="px-6 py-4 text-right"><a href="#" class="text-slate-600 hover:text-slate-800 hover:underline">Télécharger</a></td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

        {% else %}
             <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-10 text-center">
                <p class="text-gray-500">Onglet inconnu.</p>
            </div>
        {% endif %}
    </div>
{% endblock %}
